{
  "name": "cowboycodr.github.io",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "deploy": "npm run build && npx gh-pages -d build"
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "gh-pages": "^3.2.3",
    "svelte": "^3.42.6"
  },
  "type": "module"
}
